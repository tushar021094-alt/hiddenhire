import type { Job } from "@/lib/types";
export type RecruiterJobInput={title:string;description:string;companyName:string;website?:string;location?:string;city?:string;region?:string;country?:string;remote?:boolean;workplaceType?:"Remote"|"Hybrid"|"On-site"|"Unknown";salaryMin?:number;salaryMax?:number;currency?:string;experienceMin?:number;experienceMax?:number};
export function normalizeRecruiterJob(input:RecruiterJobInput):Job{return{id:"recruiter-preview",title:input.title,company:input.companyName,description:input.description,location:input.location||"Location not disclosed",city:input.city,region:input.region,country:input.country,remote:Boolean(input.remote),workplaceType:input.workplaceType??"Unknown",salaryMin:input.salaryMin,salaryMax:input.salaryMax,currency:input.currency,source:"HiddenHire",url:"#",posted:"Just posted",skills:[],indiaEligible:input.country==="India"||/india/i.test(input.location??"")||Boolean(input.remote)};}
